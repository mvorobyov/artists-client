<h2>{{model.name}}'s albums</h2>
<table>
  <tbody>
    {{#each model.albums as |album|}}
      <tr>
        <td><img src={{album.artwork_url_100}}></td>
        <td>{{album.name}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>